package ds.sort;

public class List {
    // LinkedList 算法应用
    // 对于队列 先进先出 后进后出 调用offer（add）从队尾入队 poll（unlinkFirst）从队头出队 peek（first）查看队头元素
    // 对于栈 后进先出 调用push（addFirst）从栈顶入栈 调用pop（removeFirst）从栈顶出栈 peek（first）查看栈顶元素 为什么？因为push是执行的
}
